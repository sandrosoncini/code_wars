// Given an array of words:
// const words = ['hello', 'hi', 'good', ‘hey'];
// Write some code that generates an object that looks like:
// { hello: 5, ‘hi': 2, good: 4, hey: 3 }
// Where the keys are the words and the values are the number of characters
// in each word.

// const words = ['hello', 'hi', 'good', 'hey'];

// function arrayToObj(arr) {
//    let obj ={}
//     arr.forEach(ele => {
//        obj[ele] = ele.length
//     });

//      console.log(obj)
// }


// arrayToObj(words)

let x = 7;
while(x > 5) {
console.log(x);
x = x + 1;
}
